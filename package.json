{
  "name": "bookkeeping",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:mobile": "pnpm --filter mobile dev",
    "build:shared": "pnpm --filter shared build",
    "lint": "pnpm -r lint"
  }
}
